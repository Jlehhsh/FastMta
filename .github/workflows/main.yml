      - name: Install FiveM
        env:
          DROPLET_IP: ${{ steps.create-vps.outputs.droplet_ip }}
        run: |
          # Script para instalar o FiveM na VPS
          ssh root@$DROPLET_IP 'bash -s' << 'EOF'
            # Comandos para instalar o FiveM
            apt-get update
            apt-get install -y <pacotes-necessários>
            # Configuração específica do servidor FiveM
            # Exemplo: baixar e configurar o FiveM
          EOF
